@page "/glossary/english"
@rendermode InteractiveServer

@using System.Linq
@using GlosApp.Models
@inject ApplicationDbContext DbContext

<div class="container">
    <h3>Glosformulär Engelska</h3>

    <EditForm Model="userAnswerForm" OnValidSubmit="CheckAnswers" novalidate autocomplete="off">
        @foreach (var answer in userAnswerForm.Answers)
            {
                <div class="form-group">
                    <label>@answer.Swedish</label>
                    <InputText @bind-Value="answer.UserAnswer" class="form-control" autocomplete="off" />
                    <button type="button" class="btn btn-info ms-2 mt-2" @onclick="() => ExplainWord(answer)">
                        Förklara
                    </button>

                @if (currentExplanationWord?.Swedish == answer.Swedish)
                    {
                        <div>
                            <AiResponse @ref="aiComponent" UserMessage="@userMessage" ApiToken="@apiToken" OnResponseReceived="HandleResponse" />
                        </div>

                    }


                    @if (showResult)
                    {
                        if (string.IsNullOrWhiteSpace(answer.UserAnswer))
                        {
                            <span class="text-warning">⚠️ Detta fält är tomt.</span>
                        }
                        else if (answer.UserAnswer.Equals(answer.English, StringComparison.OrdinalIgnoreCase))
                        {
                            <span class="text-success">✔️ Rätt!</span>
                        }
                        else
                        {
                            <span class="text-danger">❌ Fel, rätt svar är "@answer.English".</span>
                        }
                    }
                </div>
            }


        @if (showResult)
        {
            <div class="alert alert-info">
                Du fick @correctAnswers av @userAnswerForm.Answers.Count ord rätt!
            </div>
        }

        @if (missingAnswers)
        {
            <div class="alert alert-warning">
                Du måste fylla i alla fält innan du kan kontrollera dina svar.
            </div>
        }

        <button type="submit" class="btn btn-primary">Kontrollera svar</button>
        <button type="button" class="btn btn-success" @onclick="ResetForm">Rensa glosformuläret</button>
    </EditForm>

</div>

@code {
    private UserAnswerForm userAnswerForm = new UserAnswerForm();
    private int correctAnswers = 0;
    private bool showResult = false;
    private bool missingAnswers = false;
    private AiResponse? aiComponent;
    private string userMessage = "";
    private string apiToken = "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";


    protected override void OnInitialized()
    {
        userAnswerForm.Answers = new List<WordAnswer>
        {
new WordAnswer("på natten", "at night"),
new WordAnswer("till slut", "in the end"),
new WordAnswer("var tvungen", "had to"),
new WordAnswer("det fanns", "there were"),
new WordAnswer("folkstam", "tribe"),
new WordAnswer("bara", "only"),
new WordAnswer("överleva", "survive"),
new WordAnswer("visade", "showed"),
new WordAnswer("jagade", "hunted"),
new WordAnswer("berättelse", "story"),
new WordAnswer("åt", "ate"),
new WordAnswer("odlade", "farmed"),



        };
    }

    private async Task GetAIResponse()
    {
        if (aiComponent is not null)
        {
            await aiComponent.GetAIResponse();
        }
    }


    private WordAnswer? currentExplanationWord = null; // Håller koll på vilket ord som förklaras

    private async Task ExplainWord(WordAnswer answer)
    {
        aiResponseMessage = ""; // Rensa tidigare AI-svar
        userMessage = $"Sätt ordet i en mening {answer.Swedish}";

        currentExplanationWord = null;
        await Task.Delay(50);
        currentExplanationWord = answer;
        StateHasChanged();

        await GetAIResponse(); // Vänta på AI-responsen innan du sätter det nya ordet


    }


    private string aiResponseMessage = ""; // Variabel för att lagra AI:s svar

    private void HandleResponse(string response)
    {
        aiResponseMessage = response;
        StateHasChanged(); // UI ska uppdateras direkt
        Task.Delay(10).ContinueWith(_ => InvokeAsync(StateHasChanged)); // Testa en extra UI-refresh
    }



    private void CheckAnswers()
    {
        missingAnswers = userAnswerForm.Answers.Any(answer => string.IsNullOrWhiteSpace(answer.UserAnswer));

        if (missingAnswers)
        {
            showResult = false;
            return;
        }

        correctAnswers = userAnswerForm.Answers.Count(answer =>
            answer.UserAnswer?.Trim().Equals(answer.English.Trim(), StringComparison.OrdinalIgnoreCase) == true);

        foreach (var answer in userAnswerForm.Answers)
        {
            DbContext.WordAnswers.Add(new WordAnswer
                {
                    Swedish = answer.Swedish,
                    English = answer.English,
                    UserAnswer = answer.UserAnswer,
                    Language = "English"
                });
        }

        DbContext.SaveChanges();
        showResult = true;
    }


    private void ResetForm()
    {
        foreach (var answer in userAnswerForm.Answers)
        {
            answer.UserAnswer = string.Empty;
        }

        correctAnswers = 0;
        showResult = false;
        missingAnswers = false;
    }

    public class UserAnswerForm
    {
        public List<WordAnswer> Answers { get; set; } = new List<WordAnswer>();
    }
}




@* @page "/sofia"
@rendermode InteractiveServer
@using System.Linq
@using GlosApp.Models
@inject ApplicationDbContext DbContext

<div class="container">
    <h3>Glosformulär Engelska</h3>

    <EditForm Model="userAnswerForm" OnValidSubmit="CheckAnswers" novalidate autocomplete="off">
        @foreach (var answer in userAnswerForm.Answers)
        {
            <div class="form-group">
                <label>@answer.Swedish</label>
                <InputText @bind-Value="answer.UserAnswer" class="form-control" autocomplete="off" />
                @if (showResult)
                {
                    if (string.IsNullOrWhiteSpace(answer.UserAnswer))
                    {
                        <span class="text-warning">⚠️ Detta fält är tomt.</span>
                    }
                    else if (answer.UserAnswer.Equals(answer.English, StringComparison.OrdinalIgnoreCase))
                    {
                        <span class="text-success">✔️ Rätt!</span>
                    }
                    else
                    {
                        <span class="text-danger">❌ Fel, rätt svar är "@answer.English".</span>
                    }
                }
            </div>
        }

        @if (showResult)
        {
            <div class="alert alert-info">
                Du fick @correctAnswers av @userAnswerForm.Answers.Count ord rätt!
            </div>
        }

        @if (missingAnswers)
        {
            <div class="alert alert-warning">
                Du måste fylla i alla fält innan du kan kontrollera dina svar.
            </div>
        }

        <button type="submit" class="btn btn-primary">Kontrollera svar</button>
        <button type="button" class="btn btn-success" @onclick="ResetForm">Rensa glosformuläret</button>
    </EditForm>

</div>

@code {
    private UserAnswerForm userAnswerForm = new UserAnswerForm();
    private int correctAnswers = 0;
    private bool showResult = false;
    private bool missingAnswers = false;

    protected override void OnInitialized()
    {
        userAnswerForm.Answers = new List<WordAnswer>
        {
        new WordAnswer("trött", "tired"),
        new WordAnswer("fot", "foot"),
        new WordAnswer("tog", "took"),
        new WordAnswer("skulle", "would"),
        new WordAnswer("lys på", "shine on"),
        new WordAnswer("tjuv", "thief"),
        new WordAnswer("sprang iväg", "ran off"),
        new WordAnswer("ficklampa", "flashlight"),
        new WordAnswer("räcka, ge", "pass"),
        new WordAnswer("titta efter", "have a look")


        };
    }




    private void CheckAnswers()
    {
        missingAnswers = userAnswerForm.Answers.Any(answer => string.IsNullOrWhiteSpace(answer.UserAnswer));

        if (missingAnswers)
        {
            showResult = false;
            return;
        }

        correctAnswers = userAnswerForm.Answers.Count(answer =>
            answer.UserAnswer?.Trim().Equals(answer.English.Trim(), StringComparison.OrdinalIgnoreCase) == true);

        foreach (var answer in userAnswerForm.Answers)
        {
            DbContext.WordAnswers.Add(new WordAnswer
                {
                    Swedish = answer.Swedish,
                    English = answer.English,
                    UserAnswer = answer.UserAnswer,
                    Language = "English"
                });
        }

        DbContext.SaveChanges();
        showResult = true;
    }


    private void ResetForm()
    {
        foreach (var answer in userAnswerForm.Answers)
        {
            answer.UserAnswer = string.Empty;
        }

        correctAnswers = 0;
        showResult = false;
        missingAnswers = false;
    }

    public class UserAnswerForm
    {
        public List<WordAnswer> Answers { get; set; } = new List<WordAnswer>();
    }
}
 *@